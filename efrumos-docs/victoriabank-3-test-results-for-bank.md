# VictoriaBank e-Gateway — результаты 3 тестовых сценариев

**Мерчант:** Solvex Lux SRL, `www.efrumos.md`
**Терминал (TID):** `49807132`
**Merchant ID (MID):** `498000049807132`
**Тестовый контур:** `https://ecomt.victoriabank.md/cgi-bin/cgi_link`
**Дата тестирования:** 10.09.2026
**Тестовая карта:** `5298 5899 8959 7523` (из онбординг-письма)

Прогнаны 3 сценария, запрошенные банком. Ниже — RRN каждой транзакции и
детали.

---

## Тест 1 — успешная оплата (`TRTYPE=0` → `TRTYPE=21`)

Авторизация, затем финализация (капчур).

| Поле | Значение |
|---|---|
| ORDER | `002975` |
| **RRN** | **`625310017766`** |
| INT_REF | `2CCCF87451D2B1CC` |
| APPROVAL | `604185` |
| AMOUNT | `230.00 MDL` |
| Результат авторизации | `ACTION=0`, `RC=00` |
| Результат капчура (`TRTYPE=21`) | `RC=00`, `TEXT=Approved` |
| Подпись банка (`P_SIGN`) | проверена на нашей стороне ✅ |
| Статус заказа | оплачен |

---

## Тест 2 — авторизация + отмена без списания (`TRTYPE=0` → `TRTYPE=24`)

Авторизация без капчура, затем отмена авторизации (`TRTYPE=24`).

| Поле | Значение |
|---|---|
| ORDER | `002976` |
| **RRN** | **`625310017953`** |
| INT_REF | `027CC83CC70B9856` |
| APPROVAL | `604186` |
| AMOUNT (авторизация) | `230.00 MDL` |
| Результат авторизации | `ACTION=0`, `RC=00` |
| Результат отмены (`TRTYPE=24`) | `ACTION=0`, `RC=00`, итоговая сумма транзакции `0.00` |
| Подпись банка (`P_SIGN`) | проверена на нашей стороне ✅ |

Капчур (`TRTYPE=21`) не выполнялся — блокировка средств снята полностью,
списания не было.

---

## Тест 3 — полный цикл с возвратом (`TRTYPE=0` → `TRTYPE=21` → `TRTYPE=24`)

Авторизация, финализация (капчур), затем возврат уже списанной суммы.

| Поле | Значение |
|---|---|
| ORDER | `002978` |
| **RRN** | **`625310018219`** |
| INT_REF | `875EBFBA95D99C35` |
| APPROVAL | `604187` |
| AMOUNT | `120.00 MDL` |
| Результат авторизации | `ACTION=0`, `RC=00` |
| Результат капчура (`TRTYPE=21`) | `RC=00`, `TEXT=Approved` |
| Результат возврата (`TRTYPE=24`) | `ACTION=0`, `RC=00`, сумма возврата `120.00 MDL` |
| Подпись банка (`P_SIGN`) | проверена на нашей стороне ✅ |

Сумма теста 3 меньше (120 MDL вместо 230) — при попытке провести на 230 MDL
терминал вернул `61 — Exceeds amount limit` (исчерпан суточный лимит
тестовой карты после тестов 1 и 2). Транзакция с меньшей суммой прошла
штатно.

---

## Сводка

| Сценарий | RRN | Результат |
|---|---|---|
| Тест 1 (`0`→`21`) | `625310017766` | ✅ `RC=00` |
| Тест 2 (`0`→`24`) | `625310017953` | ✅ `RC=00` |
| Тест 3 (`0`→`21`→`24`) | `625310018219` | ✅ `RC=00` |

Все три сценария отработали успешно, подпись `P_SIGN` во всех ответах
банка проверена. Со стороны сайта интеграция готова к переключению на
боевой контур — просим прислать боевой адрес эндпоинта (аналог
`vb059.vb.md`, если отличается) и подтвердить, что боевой терминал
`49807132` активирован для приёма реальных платежей.
